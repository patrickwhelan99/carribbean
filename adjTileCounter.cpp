#include "custom.h"

adjTileCounter::adjTileCounter(int adjSea, int adjLand, int adjSand, int adjJungle, int adjLake, int adjTown, int adjMountain)
{
    this->adjSea = adjSea;
    this->adjLand = adjLand;
    this->adjSand = adjSand;
    this->adjJungle = adjJungle;
    this->adjLake = adjLake;
    this->adjTown = adjTown;
    this->adjMountain = adjMountain;
}
